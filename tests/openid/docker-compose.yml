version: '3.7'

services:
  openid-test:
    image: agent-openid
    build:
      context: ./tests/openid/src
      dockerfile: Dockerfile
    networks:
      - sca-agent
    depends_on:
      - reverse-proxy
      - scan-runner
      - source-resolver