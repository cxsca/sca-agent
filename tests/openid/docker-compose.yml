version: '3.7'

services:
  heath-test:
    image: agent-openid
    build:
      context: ./tests/openid/src
      dockerfile: src/Dockerfile
    networks:
      - sca-agent
    depends_on:
      - reverse-proxy
      - scan-runner
      - source-resolver