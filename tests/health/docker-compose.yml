version: '3.7'

services:
  heath-test:
    image: agent-health-check
    build:
      context: ./tests/health/src
      dockerfile: Dockerfile
    networks:
      - sca-agent
    depends_on:
      - reverse-proxy
      - scan-runner
      - source-resolver